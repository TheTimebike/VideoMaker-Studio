from tkinter import *
from videomaker.interface.newthemewindow.interface.entry.entry import initEntry
from videomaker.interface.newthemewindow.interface.button.button import initButton

def initWindow(focus):
    initButton(focus)
    initEntry(focus)
